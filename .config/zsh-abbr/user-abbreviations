abbr "aws account id"="aws sts get-caller-identity --query 'Account' --output text"
abbr "aws ec2 ls"="aws ec2 describe-instances --query 'Reservations[].Instances[].Tags[?Key==\`Name\`].Value' --output text"
abbr "d"="docker"
abbr "docker bash"="docker-sh bash"
abbr "docker cl"="docker system prune -af --volumes"
abbr "docker clean"="docker system prune -af --volumes"
abbr "docker co"="docker compose"
abbr "g"="git"
abbr "git brcl"="git fetch --prune; git branch | grep -v -e '*' -v -e 'master' -v -e 'main' | xargs -L1 git branch -d"
abbr "git cof"="git branch | grep -v \* | fzf --ansi --height 20% | xargs git switch"
abbr "git p"="git push"
abbr "git s"="git status"
abbr "git swf"="git branch | grep -v \* | fzf --ansi --height 20% | xargs git switch"
abbr "gp"="git push"
abbr "hadolint"="docker run --rm -i hadolint/hadolint <"
abbr "ll"="eza -lah"
abbr "make"="gmake"
abbr "pull"="git pull"
abbr "push"="git push"
abbr "t"="tmux"
abbr "tmux a"="tmux attach -t"
abbr "tmux n"="tmux new -s"
abbr "v"="nvim"
abbr "vi"="nvim"
abbr "vim"="nvim"
